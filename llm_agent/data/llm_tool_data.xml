<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <record id="llm_tool_record_retriever" model="llm.tool">
            <field name="name">retrieve_records</field>
            <field name="description">Retrieves records from Odoo database based on model name, domain filters, and field selection</field>
            <field name="service">odoo_record_retriever_tool</field>
            <field name="default">True</field>
            <field name="schema">{
                "type": "object",
                "properties": {
                    "model": {
                        "type": "string",
                        "description": "The Odoo model name (e.g., 'res.partner', 'product.product')"
                    },
                    "domain": {
                        "type": "array",
                        "items": {
                            "type": "array"
                        },
                        "description": "A domain filter as a list of criteria (e.g., [['is_company', '=', true]])"
                    },
                    "fields": {
                        "type": "array",
                        "items": {
                            "type": "string"
                        },
                        "description": "Fields to retrieve (e.g., ['name', 'email']). If empty, all fields will be retrieved."
                    },
                    "limit": {
                        "type": "integer",
                        "description": "Maximum number of records to retrieve (default: 100)"
                    }
                },
                "required": ["model", "domain"]
            }</field>
        </record>
    </data>
</odoo>